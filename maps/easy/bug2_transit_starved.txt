# Repro map for bug #2: a drone already committed to a restricted-zone
# transit (reserved last turn, obligated to land THIS turn) gets blocked
# because a fresh drone arriving at the same junction the same turn
# consumes the connection's per-turn capacity first.
#
# D1 takes the short branch (start-s1-mid), D2 takes the long branch
# (start-l1-l2-mid); both funnel through the single-slot mid->r_zone
# connection. D1 starts its transit one turn ahead of D2's arrival, so by
# the turn D1 is *required* to land, D2 shows up fresh at "mid" and,
# because it is further along its own path, gets processed first and
# steals the connection's only slot.
#
# Watch: D1 shows "In transit: mid -> r_zone" for TWO consecutive turns
# instead of landing on the very next turn as the subject requires
# ("It can't wait extra turns on the connection").
nb_drones: 2
start_hub: start 0 0
hub: s1 1 0
hub: l1 1 1
hub: l2 2 1
hub: mid 3 0 [max_drones=2]
hub: r_zone 4 0 [zone=restricted max_drones=5]
end_hub: goal 5 0
connection: start-s1
connection: s1-mid
connection: start-l1
connection: l1-l2
connection: l2-mid
connection: mid-r_zone [max_link_capacity=1]
connection: r_zone-goal
