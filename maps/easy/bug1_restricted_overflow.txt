# Repro map for bug #1: a restricted zone accepts a reservation while it is
# already at full capacity. R has max_drones=1: D1 lands in R, and on the
# very same turn D2 (right behind it) is wrongly granted a transit
# reservation into R even though R already holds D1.
#
# Watch turn 3 in the output: the "r_zone" line shows D1 present, while
# "In transit" shows pre -> r_zone: D2 in the same snapshot -- D2 should
# never have been allowed to reserve a slot in an already-full zone
# (max_drones=1).
nb_drones: 2
start_hub: start 0 0
hub: pre 1 0
hub: r_zone 2 0 [zone=restricted max_drones=1]
end_hub: goal 3 0 [zone=restricted]
connection: start-pre
connection: pre-r_zone
connection: r_zone-goal
